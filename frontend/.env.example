# VITE_ variables are public and included in the browser bundle.
# Leave blank for same-origin /api requests. Set an explicit public API origin
# at build time when the frontend and API are deployed on different origins.
VITE_API_BASE_URL=
